# Session Handoff: 2026-07-13 (v16)

## Summary

Autonomous session (owner away ~2h, asked to pick up whatever made sense and
make progress without input). The shippable backlog was gated: of the six open
issues, five are `design-call` or `playtest-gated`. The one clearly-actionable,
non-gated issue was #167 (explore repo-local Claude Code skills), so this session
resolved it: built two skills, declined a third, and landed the recommendation.
Then spent the remaining time on the highest-leverage non-code work: the design
pass #111 explicitly requires before it can be built, and tracker hygiene.

`main` is clean and green. PR #196 merged (CI green). This handoff PR adds a
CLAUDE.md pointer to the new skills.

Live game: https://ricschuster.github.io/Courier-of-the-Borderlands/

## Completed

- **#167 resolved (PR #196, merged).** Added two repo-local skills under
  `.claude/skills/`:
  - `region-map` - front-loads the region-authoring guardrails that edits keep
    tripping over: the reachability/sealed-pocket invariants
    (`region-invariants.test.ts`), the wear/travel-sink estimate and the
    `wearMultiplier` lever, and the trap where `test:arc` misses the
    coordinate-hardcoding region specs (so you must run the full chromium e2e).
  - `playtest-triage` - turn a raw playtest note dump into bucketed issues
    (fix-now / design-call / playtest-gated / tech-debt) and ship the clear fixes
    one PR at a time.
  - **Declined** a "write + land a handoff" skill: CLAUDE.md already carries the
    format and the auto-land flow, so a skill would be a drifting duplicate. The
    recommendation and the "where skills end and docs begin" answer live in
    `.claude/skills/README.md`.
- **#111 design pass posted (issue comment).** The issue asks for a design pass
  before building; delivered it grounded in the code. Key finding: a hidden
  *shortcut* is the weakest option (you can't desire a route you can't see, which
  fights the whole roads-gate pull) and is the hardest gate to keep the arc
  honest about (reveal is a soft, path- and radius-dependent property, unlike a
  passability true/false). Recommendation: if #111 happens at all, reframe it as a
  reveal-rewarded lore/Cipher *discovery*, not a route gate, and only after the
  #109 playtest shows reveal skills still feel like idle buffs. Owner to pick
  1/2/3 or close.
- **#153 tracker tidied.** Posted a session-update comment: #151 is shipped/closed
  (its checkbox was stale), so no fix-now work is buried there; remaining items
  are all owner-gated.
- **CLAUDE.md pointer to the skills** (this PR) so future sessions discover them
  instead of re-deriving the steps.

## Current state

What works:
- `main` clean and green; #167 shipped.
- The two new skills are usable now via their names (`region-map`,
  `playtest-triage`).

What is incomplete / gated (unchanged, all owner-gated):
- **#111** reveal-hidden routes - design pass posted, awaiting owner's pick.
- **#152** art Phase 2 - design call.
- **#121** demote the Arc - tech-debt/design-call. Memory stance is "Arc stays
  blocking, #121 is the fallback"; left untouched (not pressing).
- **#109 / #110** - fresh full-arc playtest and economy tightening, both want a
  human play signal.

## Next actions (open issues)

All owner-gated, none urgent:

1. **#111** pick from the posted design pass (reframe as lore discovery / build a
   hidden shortcut / close as not worth it).
2. **#109** owner full-arc playtest of the current build (unblocks #110 and #111).
3. **#152** art Phase 2 decision.
4. **#121** decide whether to demote the Arc (or close, keeping it blocking).

## Risks or decisions needed

- Nothing shipped this session needs a human read; the two skills are process
  docs with no game-logic effect. The `region-map` skill's one drift risk is if
  the region test file names or the `test:arc` behaviour change; it points at the
  source of truth rather than copying values to fail loud if so.
