# Session Handoff: 2026-07-26 (v40)

## Summary

Close-out for the session v39 documents. **Read v39 first**: it carries the full
narrative of the five #392 slices, the harness fix, and the decisions. This adds
only what v39 could not know, because it was written before its own PR landed.

The state to start from: `main` is green, everything mergeable is merged, and for
the first time in this thread **there is no unblocked work left that does not need
an owner decision or a playtest**.

## Completed since v39

- **PR #403 merged**, carrying v39 itself plus ADR 0009, CLAUDE.md traps 6 and 7,
  and the `docs/decisions/README.md` naming correction.
- **#363 now carries a practical brief** (a comment on the issue): where to play,
  the three setup gotchas, how long a useful pass takes, what to listen for, and
  how to report. It previously said what to judge but not how to run it, which is
  part of why it sat through two sessions.

## Current state

- `main` green at `207d215`. Nine PRs merged across the session (#381, #391, #394,
  #396, #398, #399, #400, #401, #402, #403).
- 0.8.0 is the released version and is live. The whole refactor sits above it
  unreleased, and none of it is player-visible.
- `map-scene.ts` **1939 lines**, from 2372 at session start. Unit suite **1170**
  (from 1120), browser **50** (from 49).
- **Four open issues, and all four are now owner-gated:**
  - #363 human playtest on 0.8.0 (`playtest-gated`) - has a brief now; unblocks #361
  - #392 `map-scene.ts` slicing (`tech-debt`) - the cheap clusters are done; what
    remains needs design calls, see ADR 0009
  - #362 arc completes with zero voluntary spending (`design-call`)
  - #361 Wayfinder economics (`design-call`, `playtest-gated`)

## Next actions

1. **#363.** The brief is on the issue. Twenty to thirty minutes of play with the
   sound on covers most of it; the full arc is only needed for the Wayfinder
   question and "is it fun".
2. **Decide whether `create()` wants its own module** before any slice six. It is
   around 250 lines and builds four host literals. ADR 0009 leaves this open
   deliberately and flags it as the likely next ADR.
3. Decide #362.
4. Consider a release, though there is nothing player-visible above 0.8.0.

## Risks or decisions needed

- **The audio has still not been heard at volume**, now across three handoffs. It
  is shipped to players. The bed remains the prime suspect at roughly a third of
  its commanded level, and retuning stays cheap only until #226 Phase 2 lands real
  samples.
- **There is no unblocked work left.** A session starting from here and looking for
  something mechanical to do will not find it, and should not invent it. The
  honest options are: run the playtest, take a design call, or take a #392 cluster
  knowing it needs judgement rather than pattern-following.
- **`create()` is the next `map-scene.ts` if the pattern continues unchanged**, and
  it carries a constraint that fails at runtime rather than compile time: two
  controllers are built before the HUD exists, which works only because every host
  member reads through a closure.
