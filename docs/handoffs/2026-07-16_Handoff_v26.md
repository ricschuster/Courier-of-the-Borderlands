# Session Handoff: 2026-07-16 (v26)

## Summary

Continues [v25](2026-07-15_Handoff_v25.md). No game work. This session did the two
things v25 predicted and left open: it emptied this machine's personal memory store,
and it cleaned up `CLAUDE.md`.

v25's Next Action 1 was "the other subscription's memory store has not been touched
and is likely carrying the same staleness... expect it to produce a PR if it finds
an orphan." That store is this machine's. Audited it the same way, one file at a
time against the code. Unlike the other machine's 20-entry store, this one held **3
entries, all fully duplicated in `CLAUDE.md`, zero orphans**, so nothing was
promoted and no rescue PR was needed. The predicted-orphan case did not fire here.

Then, prompted by the owner, reviewed `CLAUDE.md` itself and shipped a cleanup: one
real correctness fix plus a trim of shipped-MVP scaffolding.

One PR merged (#289). Open issues unchanged at 3.

Timestamps are 2026-07-16 UTC; local date rolled over as in v24/v25.

## Completed

- **Personal memory store emptied on this machine.** 3 content files
  (`auto-commit-handoffs`, `ci-test-focus-is-intentional`,
  `gameplay-direction-less-forgiving`) read in full, grep-verified against
  `CLAUDE.md`, and deleted. All 3 were owner-direction/process facts already living
  in `CLAUDE.md` (the same consolidation v25 did on the other machine). Store now 2
  files: an index and a guard, both saying it is empty by design. This is a
  Claude-local store, not in git, so there is no commit for it.
- **#288 -> PR #289.** `CLAUDE.md` cleanup, two commits.

## The CLAUDE.md cleanup

The file was 618 lines and stratified by age: the first half was kickoff
scaffolding describing an MVP shipped in 0.4.0, the second half current operational
rules. A session reads all of it every turn.

- **Correctness fix (commit 1).** `CLAUDE.md` recommended JSON as the canonical
  game-data format in four places, but there is no JSON game data: all 15 modules
  in `src/data/` are typed TypeScript. This is the aspirational-JSON trap the file
  warns about in trap 2, and what made the closed #225 and #229 unbuildable. Stack
  line, file-naming examples, Data rules, and the TypeScript rule now all say typed
  TS modules, with JSON an explicit non-goal pending an ADR.
- **Structural trim (commit 2).** Retired "Current MVP target", "MVP definition of
  done", "Initial game systems", and "Build order" (a met target, recorded
  milestone-by-milestone in `docs/design/02_milestones.md`) to one "Project status"
  pointer. "What this game is / is not" tightened to "Scope". Fictional
  `game-project/` ASCII tree replaced with an accurate description. Memory-store
  post-mortem trimmed to a pointer to the guard file.
- Result: 618 -> 512 lines, no operational rule removed.

## Decisions

- **Verify each memory, do not trust the ratio.** The owner's prompt gave the other
  machine's shape (20 entries, 2 orphans) but said explicitly not to trust it. Good
  call: this store was different (3 entries, 0 orphans). Bulk-deleting on the "all
  redundant" prior would have been safe here by luck, but the one real risk is a
  memory that lives nowhere else, and only per-file checking rules that out.
- **Held the CLAUDE.md trim at 512, not the ~400 first floated.** Left the four
  process sections ("Issue workflow", "Where durable context lives", "Session
  start", "Repo-local skills") unmerged. They circle one theme but each carries
  distinct load-bearing nuance; merging risked quietly dropping a rule, which is
  worse than a few extra lines. Only de-duplicated the one verbatim-repeated phrase.
- **Kept the correctness fix and the trim as separate commits** so the JSON change
  is reviewable apart from the reshuffle.

## Current state

- `main` green at 2dca181. 0.4.0 released and deployed. Working tree clean apart
  from the untracked `telemetry-seed.*` files (still v1 schema, still ignored, still
  harmless; delete when convenient).
- **3 open issues, all owner-gated and unchanged since v24**: #226 (audio), #228
  (colorblind palette), #229 (visual region editor). #228 remains the smallest and
  most concrete. See v24 for per-issue detail.
- Personal memory store: 2 files, intentionally empty, on both machines now.
- No `src/` changes this session, so the coverage-gate margin is unchanged.

## Next actions

No tractable backlog. All three open issues need an owner decision first; none
should be picked up unprompted. See [v24](2026-07-15_Handoff_v24.md).

1. **Optional CLAUDE.md follow-up:** a more aggressive consolidation of the four
   process sections, if the owner wants to eyeball the merge. Deliberately deferred
   this session to avoid losing nuance blind.

## Risks or decisions needed

- **Audit the rest of `scripts/` and any tool with no CI job** (carried from v25,
  now trap 5 in `CLAUDE.md`). Not revisited this session.
- **`googleapis/release-please-action@v4` still targets deprecated Node 20**
  (carried from v24/v25). Works today, watch on release runs, nothing actionable
  upstream yet.
