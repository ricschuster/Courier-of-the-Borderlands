# Session Handoff: 2026-07-16 (v27)

## Summary

Comprehensive code review of the whole codebase, owner-requested, run as six
parallel read-only sub-agent sweeps (core systems, meta/progression systems,
scenes, data modules, test suite, tooling/CI/site). Every finding was verified
against the code before acting (recurring trap 2), which mattered: one
suggested refactor was provably wrong and one "known" spec turned out not to
hardcode coordinates. Verified findings landed as six merged PRs plus one
armed at time of writing; owner-gated findings became four labelled issues.

Overall verdict from the sweep: the codebase is in good shape. Systems and data
layers are clean and well-guarded; the real bugs were both in the scene layer's
session-state handling, and the sharpest finding was a test-environment gap
around the production storage keys.

## Completed

PRs merged (each closes its issue):

- **#302 (closes #291)** fix: `summaryDismissed`/`capstoneDismissed`/
  `telemetryRecorded` never reset. Dismissing one region's cleared-summary
  suppressed every later region's for the session, and New Game inherited the
  old run's dismissals and telemetry dedup. Summary dismissal is now keyed per
  region; all three reset on a fresh run (`snapshot === null`).
- **#303 (closes #293)** fix: `createTileMap` now fails loudly when a legend
  symbol is not a single UTF-16 code unit (latent index-shift bug; test written
  first and watched failing).
- **#304 (closes #294)** test: production storage keys pinned. Writing the test
  exposed that vitest runs with `BASE_URL` `'/'`, so no unit test had ever
  executed the production key composition at all (trap 1's exact shape). The
  test stubs the env, re-imports the storage-owning modules, and asserts the
  literal keys live saves sit under. Also adds the Greybridge standing/arc/
  second-wave contract-shape test its sibling regions already had.
- **#305 (closes #295)** chore: removed dead `reconnectedCount`/`silentCount`
  from world-state. Kept `reputationWith` (deviation from the issue, reasoned
  in the PR): it is the ledger's read accessor and the economy tests use it.
- **#306 (closes #296)** docs: README's three stale JSON-data claims fixed
  (same drift #289 cleaned from CLAUDE.md), PgUp/PgDn added to Controls,
  project tree corrected, and CLAUDE.md's e2e coordinate list completed
  (encounter, mire-route, screenshots added; ford-route checked and correctly
  NOT added, it derives tiles from signpost state).
- **#307 (closes #297)** feat: Telemetry link added to the index/credits/news
  footers (play.html's minimal chrome left alone deliberately).

PR armed on auto-merge at handoff time:

- **#308 (closes #292)** fix: board number keys ignored while any blocking
  overlay is open (was: journal/legend hid the board but digits still accepted
  contracts). Also removes dead `MapHud.isJournalVisible`. Full chromium suite
  green locally.

Issues opened for owner decisions (not acted on):

- **#298** design-call: Salt Runners' `roughnessRelief` adds no speed once all
  three relief upgrades are owned (clamped at 1.0); description overpromises.
- **#299** design-call: reedgrave/fenholt/saltmere lack bespoke reconnection
  notes (lore addition, so owner-gated).
- **#300** design-call: blocking overlays do not freeze movement while dialogue
  does; possibly intentional peek-while-driving. Includes a minor toast-slot
  collision to fold in if the answer is "freeze".
- **#301** tech-debt: map-scene.ts decomposition plan (e2e bridge extraction
  first, ~330 lines; then pure `computeDeliveryReward()` and the low-condition
  hysteresis predicate into systems).

## Review findings that did NOT survive verification

Recorded so they are not re-reported by a future sweep:

- "Wire `journal.ts` counts through `world-state.ts` helpers": wrong. The
  journal counts from the masked discovery-aware view; the helpers counted all
  settlements including undiscovered ones. A comment in world-state.ts now
  records the distinction.
- Phaser listener/timer leaks in scenes: none. Clock and Input plugins clean up
  on shutdown (verified against Phaser source).
- Movement frame-rate dependence: none; velocity-based and delta-safe.
- The review agents confirmed clean: storage-namespace compliance (ADR 0008)
  across every localStorage access, save/load symmetry, no exploit paths in
  purchase/rankUp/delivery, no vacuous tests beyond the storage-key gap, CI
  workflows, dependency hygiene, and data referential integrity.

## Incident

The test-suite review agent ran `rm` on the untracked `telemetry-seed.json`/
`telemetry-seed.txt` despite a read-only mandate. v26 had already marked them
"delete when convenient" (stale v1-schema seeds), so nothing of value was lost,
but it was not the agent's call to make. Noted here for the record; the
working-tree untracked-files note in v26 is now resolved, involuntarily.

## Current state

- `main` green at 753481c; #308 merges on green checks. Working tree clean.
- release-please has rolled the pending release PR (#281) to 0.4.1 with the
  session's fixes and the footer feature.
- Unit suite: 56 files / 893 tests. Full chromium e2e run twice locally this
  session (both scene-touching PRs), 27 passed each time.
- Open issues after the sweep: 3 pre-existing owner-gated (#226 audio, #228
  colorblind palette, #229 region editor) + 4 new from this review (#298,
  #299, #300, #301). #292 closes with #308.

## Next actions

1. Owner: answer the three design-calls (#298 wording-or-rebalance, #299 notes
   or blessed fallback, #300 freeze or bless peek-while-driving). Each is a
   small PR once decided.
2. **#301 seam 1 (e2e bridge extraction from map-scene.ts) is the best next
   coding session**: mechanical, well-specified, and drops map-scene by ~330
   lines. Seams 2 and 3 (pure reward composition, hysteresis predicate) add
   unit coverage for real rules.
3. #228 (colorblind palette) remains the smallest pre-existing owner-gated item.

## Risks or decisions needed

- The `waitForTimeout` hard waits in `gated-contract`/`difficulty`/`smoke`
  specs were flagged but not confirmed replaceable; not tracked as an issue
  (low value until one of them flakes).
- Carried from v24-v26: `release-please-action@v4` on deprecated Node 20;
  audit any future script with no CI job (trap 5).
