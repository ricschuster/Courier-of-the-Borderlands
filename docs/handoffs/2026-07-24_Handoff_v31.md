# Session Handoff: 2026-07-24 (v31)

## Summary

A repo hygiene session: cleared the merge backlog, cut 0.6.0, and corrected two
places where the tracker and `CLAUDE.md` had drifted from reality. No gameplay
code changed.

This handoff also back-fills the 2026-07-18/19 session, which shipped seven PRs
and closed ten issues without leaving a record. That stretch is reconstructed
from git history, the issue tracker, and the release, not from first-hand
session notes, so it lists what landed and omits the reasoning behind each call.

## Completed

### Back-fill: 2026-07-18/19 (reconstructed)

Shipped from the persona-playtest triage, after handoff v30 closed:

- #337 named the real exit when stranded and too broke to repair (closes #317).
- #339 rebalanced relief upgrades so all three add speed (closes #298).
- #340 gave Cipher a secrets-cargo payout rider (closes #323).
- #341 gave Wayfinder a minimap survey ring (closes #324).
- #342 added a save indicator and enlarged the minimap (two items of #327).
- #348 added a solo build-effort estimate page (closes #347).
- 0.5.0 released on 2026-07-18 via #281.

Ten issues closed across those two days: #298, #317, #318, #322, #323, #324,
#325, #326, #328, #347. The persona harness question (#328) was settled and
`tmp-personas/` is gone from the working machine.

### This session: merge backlog and 0.6.0

Five PRs had been sitting green since 18-19 July. Merged in an order that let
release-please fold the feature work into the release rather than stranding it
for a later version:

1. #344 (release-please-action v4 to v5), #345 (setup-node 6 to 7), #346
   (dev-dependencies). The v5 bump also cleared the deprecated-Node-20 warning
   carried since handoff v30.
2. #343 colorblind-safe terrain palette, which auto-closed #228.
3. #338 release, after release-please regenerated it. The regenerated changelog
   was checked before merging: it picked up #343 alongside #337 to #342 and
   #348, and correctly left the three CI/chore bumps out of the notes.

0.6.0 is tagged, released, and deployed. Deploy, CI, Arc, Release Please, and
Security all completed successfully on `main` afterwards. The Release Please run
mattered most, because #344 upgraded the very action that generates releases;
v5 ran clean. Local gate on the new `main` with bumped dependencies: lint clean,
typecheck clean, 934 tests passing, `npm ci` reporting no vulnerabilities.

### This session: local git hygiene

- `main` fast-forwarded and level with `origin/main`.
- 32 merged local branches deleted. Squash merges mean `git branch --merged`
  cannot see them, so they were classified by PR state through `gh` instead.
  Only `main` remains locally; `origin` is down to `main` and `gh-pages`.
- `git gc --prune=now` took `.git` from 226M to 25M: loose objects went from
  221.98 MiB to zero, into a single 23.44 MiB pack. The loose objects had
  accumulated from fetching `gh-pages`, which carries every preview build.

### This session: roadmap accuracy

- **#229 (visual region editor) closed as not planned**, on the owner's call.
  Recorded on the issue: the reason is size versus payoff, *not* the JSON
  premise. That premise was already corrected in the body on 2026-07-15, which
  states that there is no JSON and that the idea survives regardless of output
  format. Closing it as "assumes JSON" would have misread it.
- **`CLAUDE.md` corrected.** The Data rules section cited "the closed #225 and
  #229" as examples of features assuming a JSON content layer. #229 was open at
  the time, and after its 2026-07-15 correction it was never a JSON-assuming
  feature, so the citation is now #225 alone.
- **#327 trimmed and retitled.** Three of its four items had shipped (#335 for
  the difficulty rows, #342 for the minimap and save indicator), leaving only
  the toast fade, which is a design call rather than a ready fix. The body now
  carries just that item plus the reason it is held, and the issue is labelled
  `design-call`.

## Current state

- `main` is green at 0.6.0 and fully deployed. No open PRs.
- Four open issues, three of which are blocked on an owner decision rather than
  on implementation: #327 (toast fade), #300 (blocking overlays do not freeze
  movement), #299 (pocket settlement reconnection notes), and #226 (audio).
- Found and not acted on:
  - `COURIER_COLOR` (`src/config/game-config.ts:21`) is exported and referenced
    nowhere, while the same gold is hardcoded in five places:
    `src/scenes/juice.ts:43`, `src/scenes/title-scene.ts:31`, and
    `src/scenes/map-hud.ts` lines 30, 309, 368, and 792. Small, safe PR.
  - `src/scenes/map-scene.ts` is 2232 lines, more than double the next largest
    file, against the "small modules" principle. `map-hud`, `map-scene-e2e`, and
    `dialogue-controller` were carved out of it already, so the pattern for
    continuing is proven: one extraction per PR, tests around the moved rules.
  - About 80 exports have no cross-file reference, but nearly all are types used
    only inside their own module. Deliberately left alone: low value, high churn.
  - `gh-pages` carries 71 empty `pr-preview/pr-N/` directories, each holding a
    single leftover `.nojekyll`. Previews do delete their assets on close, so
    this is 71 stray files and no stale published builds. Cosmetic.

## Next actions

1. The `COURIER_COLOR` cleanup, as one small PR.
2. First extraction out of `map-scene.ts`.
3. The four owner design calls: #327, #300, #299, #226.

## Risks or decisions needed

- The toast fade (#327) still reverses a hold time tuned across three sessions.
  Unchanged from v30: it needs a direction call, likely auto-expiring transient
  status toasts while narrative ones hold.
- A human playtest remains the real unblock for the tuning thread, and no
  automated run substitutes for it.
