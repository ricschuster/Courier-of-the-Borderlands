# Session Handoff: 2026-07-16 (v30)

## Summary

Turned the 2026-07-16 persona playtest's filed issues into shipped fixes.
Cleared the three bugs first, then a run of the genuinely small, safe
enhancements. Eight PRs opened, verified, and merged to `main` (all through
auto-merge on green CI). One design-call was deliberately left for the owner,
and one CI workflow (release-please) is failing on a transient GitHub API
outage, not a repo problem.

## Completed

Bugs (all from the persona playtest):

- #315 -> PR #329: page reload was a free instant tow. The save now records the
  courier tile additively; a cold boot resumes there when the tile is still
  passable, region travel still arrives at the gateway, and older saves resume
  at spawn as before. Verified both directions with the playtest repro.
- #316 -> PR #330: board digit input was not gated on the summary/capstone
  panels. Extracted a pure `boardInteractable()` predicate that both the draw
  and input paths consult, so visibility and input cannot drift apart again
  (the root of both this and the earlier #292).
- #319 -> PR #331: Esc now closes any open blocking overlay (journal, skills,
  codex, upgrade menu), matching the dialogue's "Esc to step away". The
  movement-behind-overlays half stays with design-call #300.

Enhancements (small, safe half of the playtest batch):

- #320 -> PR #332: the repair hint now reads "R: full repair 57c (5c/pt)",
  naming the full restore and the live per-point rate (varies by difficulty).
- #321 -> PR #333: the contract board now arms on the first digit-press and
  accepts only on a confirming second press of the same slot, so a remembered
  digit on a renumbered board no longer commits the wrong journey.
- #326 (partial) -> PR #334: teaches the off-road wear lesson the first time the
  wagon is driven onto rough ground. The blocked-compass route-hint half remains
  open on #326.
- #327 (partial) -> PR #335: the title difficulty rows are now clickable with a
  hover highlight. The minimap, toast fade, and save-indicator items remain open
  on #327.

Each PR was verified with the gate it warranted: unit + the relevant e2e, a
neutralize-check (trap 1) on every new behavior, and real-play screenshots for
the two visual changes (#320 repair hint, #327 title screen).

## Current state

- `main` is green and carries all eight fixes; every PR auto-merged on passing
  required checks.
- release-please run #66 (triggered by #335's merge) is FAILING, and a manual
  re-run also failed. Cause is a transient GitHub Actions API outage: the
  action gets GitHub's HTML error page instead of JSON ("release-please failed:
  <!DOCTYPE html> ... Unicorn!"). Same outage was 503-ing the CLI. It is not a
  repo or code problem; `main`, the build, and the merged PRs are unaffected.
  release-please only maintains the release PR/changelog, so a re-run once the
  API is stable will regenerate it. A background watcher was polling for API
  recovery to re-run; if it did not finish, re-run manually:
  `gh run rerun 29539661682` (or the Re-run jobs button).
- Untracked `tmp-personas/` remains on this machine (harness + one throwaway
  screenshot script added this session); adopt-or-delete is #328.

## Next actions

1. Re-run release-please (#66) once the GitHub API is stable; confirm it opens
   the release PR covering #329-#335.
2. #326 remaining half: a route hint when the compass points at a blocked pass
   (needs a reliable "misleading heading" trigger and playtest-checked wording).
3. #327 remaining items: minimap readability, a save indicator, and the toast
   fade (the toast fade is a design-call, see risks).
4. Owner design-calls the playtest fed: #298, #299, #300, #317, #318, #322-325,
   and the harness adopt-or-delete #328.

## Risks or decisions needed

- Toast fade (#327 item): deliberately NOT done. It would reverse the
  hold-until-Space toast decision tuned across three past sessions
  (docs/design/05_playtest_notes.md). Needs an owner call on direction, e.g.
  auto-expire only transient status toasts while narrative ones hold.
- Carried: release-please-action@v4 runs on deprecated Node 20 (a warning in
  run #66, not today's failure); waitForTimeout hard waits in three specs.
