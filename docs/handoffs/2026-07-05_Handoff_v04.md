# Session Handoff: 2026-07-05

## Summary

Turned the e2e suite from "the game boots" into "the game is playable" as a CI
gate, cleared backlog items 1 to 5 with parallel sub-agents, and cut down the
permission prompts that were slowing the workflow. Everything shipped as small
CI-gated PRs (#11 to #21) and is on `main` and deployed.

Live game: https://ricschuster.github.io/Courier-of-the-Borderlands/

This supersedes handoff v03 (which was written mid-session, before items 1 to 5).

## Completed

Gameplay testing:
- PR #11: input-driven e2e playthrough of the delivery loop. Drives the courier
  with real key presses in headless Chromium (reach the home town, accept a
  contract, drive to the destination, complete the delivery) and asserts rewards
  and save state. Added a read-only `window.__courier` hook, attached only when
  the game boots with `?e2e` and never in normal play (`getState()`,
  `nextStepToward(tile)`). Movement is genuine keyboard input through Phaser.
- PR #12: route-unlock coverage. Drives to the Greybridge signpost and confirms
  the southern ford opens (flag flips, unlock recorded and persisted, ford tile
  goes from impassable to passable). Extended the hook with `fordUnlocked`,
  `unlocks`, `signpost`, and `isPassableTile(x, y)`.

Backlog items 1 to 5 (fanned out to three parallel worktree sub-agents, then
integrated and verified together before merge):
- PR #16 (items 1 + 4): Fenmarch contracts now set `cargoType`
  (goods/letters/secrets); Fenmarch gained its own locked ford + signpost
  (`ford-fenmarch`, unlock `ford-crossing-fenmarch`, ford at (9,8)/(10,8),
  signpost at (8,8)), exercising the per-region ford mechanic in a second region.
- PR #14 (item 2): contract board header uses each region's home settlement name
  instead of the hardcoded "GREYWATER BOARD".
- PR #15 (item 3): Fenmarch e2e smoke test (boots from a save with
  `regionId: fenmarch`).
- PR #17 (item 5): reconciled `docs/design/02_milestones.md` (M2 and M3 marked
  DONE with corrected file paths; new M4 section for post-MVP systems).

Housekeeping and tooling:
- PR #18: added `.claude/worktrees/` to `.gitignore` and the eslint ignore list
  (agent worktrees are full repo copies that were polluting `eslint .`).
- PR #19: untracked the stale `.claude/scheduled_tasks.lock` (already gitignored,
  lingered as a tracked deletion). `git status` is now clean.
- PR #20: moved the machine-agnostic Claude Code permissions (acceptEdits default
  mode plus the safe command allowlist) into the checked-in `.claude/settings.json`
  so they transfer across machines. Machine-specific `env.PATH` stays in the
  gitignored `settings.local.json`.
- PR #21: added a "Command style" section to `CLAUDE.md` (keep shell commands
  atomic so they match the allowlist; no `source ~/.bashrc` prefix; use
  `gh pr checks <n> --watch` for CI, not poll loops).

## Current state

What works:
- Everything above. `main` is green (CI: check + e2e) and deployed.
- 321 unit tests + 5 Playwright tests (3 region smoke + 2 input-driven
  playthrough). Working tree clean.

Architecture (unchanged shape):
- Pure logic in `src/systems/` (no Phaser), unit tested. Phaser rendering/input
  in `src/scenes/map-scene.ts`. Canonical content in `src/data/`. Regions in
  `src/systems/region-system.ts`. The `?e2e` test hook lives in `map-scene.ts`
  and is inert without the flag.

Workflow notes for the next session:
- Permission prompts are much reduced. Run npm/node/npx bare (no
  `source ~/.bashrc`), keep commands atomic, and use `gh pr checks <n> --watch`
  to wait on CI. See the new CLAUDE.md "Command style" section.
- `settings.local.json` still lists the same permissions as `settings.json`
  (harmless merge); pruning the duplicates is optional cleanup.
- `env.PATH` in `settings.local.json` points Node at the user-local v24 to match
  CI; it is machine-specific and takes effect at session start. System Node v22
  also works if it is absent.

## Next actions (feature backlog)

Build one per branch/PR, CI-gated:

1. Extend the playthrough to cover a vehicle upgrade purchase at the home shop
   (drive home, buy an upgrade, assert coins spent and the upgrade applied).
   Needs `coins`/`upgrades` on the `?e2e` hook (small add).
2. Extend the playthrough to drive across the now-open ford and confirm the
   shorter southern route is actually used by pathfinding.
3. Add a Fenmarch route-unlock e2e test now that Fenmarch has a ford + signpost
   (mirror the Greybridge route-unlock test).

Optional cleanup:
- Prune the duplicate permission entries from `.claude/settings.local.json` so
  `.claude/settings.json` is the single source of truth.

Not in the auto-queue (need human input): audio and juice (asset/licensing) and
an art pass beyond grey-box.

## Risks or decisions needed

- Keep authoring regions as a linear chain (Greybridge <-> Saltreach <->
  Fenmarch) or move to a hub layout now that gateways are a list.
- Whether to flip `enforce_admins` on branch protection (hard gate vs owner
  break-glass).
- Whether to keep `env.PATH` (Node v24 parity) in `settings.local.json` or drop
  it and rely on system Node.
