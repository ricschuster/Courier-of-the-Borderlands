# Session Handoff: 2026-07-12 (v12)

## Summary

Short, focused session that resumed the paused `feat/upgrade-menu` workstream
from v11 and shipped it. The upgrade menu's core, HUD, and unit tests were
already committed as WIP (07e62d6); this session finished the e2e layer, verified
the whole suite, and merged. That closes #161 and completes the staged UI +
onboarding pass (#149) in full. `main` is green and clean.

Live game: https://ricschuster.github.io/Courier-of-the-Borderlands/

## Completed

### Selectable wagon upgrade menu shipped (#161 -> PR #163, merged)

Replaced the old "press B buys the cheapest upgrade" with a real, selectable
menu. Core + HUD + unit tests landed in the prior WIP commit; this session added
the e2e wiring that was the only thing blocking the ship:

- `E2EState.upgradeMenuOpen` exposes `hud.isUpgradeMenuVisible()` so specs can
  drive the `B` toggle deterministically.
- `drive.ts`: `setUpgradeMenu(open)` mirrors `setSkillPanel` for the `B` toggle;
  `buyAffordableUpgrades` opens the menu, presses keys `1-7` (each a no-op when
  unaffordable or already owned), then closes it.
- `upgrade.spec`: opens the menu and buys Far Lantern by its number key (`2`)
  instead of the retired bare-`B` instant buy.
- `full-arc.spec` + `travel-sink-measure.spec`: `spendAtHome` now kits out the
  wagon through the menu via `buyAffordableUpgrades`.

Squash-merged as `14e99ee`; branch deleted; #161 auto-closed.

### Tracker updated

- #153 (playtest triage checklist): ticked Bucket 3 (UI frame + onboarding pass,
  now fully shipped across #159/#160/#161/#162) and Bucket 5's UI look-and-feel
  item (folded into #149). Added a session-update note. Left open: its remaining
  items point to #151 and #152.

## Current state

What works:
- `main` green. tsc + eslint clean; 703 unit tests pass; full chromium e2e suite
  (23 pass, sink-measure skipped by default) and the full-arc arc both green.
  Working tree clean on `main`.
- The upgrade menu opens with `B` at the home shop, lists all seven Greybridge
  upgrades (cost, affordability, effect), buys by number key, closes with `B`.
  Menu rendering confirmed by screenshot this session.
- The staged UI + onboarding pass (#149) is fully landed: HUD frame (#159),
  just-in-time onboarding (#160), reserved overlay region (#162), upgrade menu
  (#161). #149 is closed.
- No open PRs; no dangling branches.

Behaviour note:
- `buyAffordableUpgrades` presses keys `1-7`, so `spendAtHome` now buys *every*
  affordable upgrade rather than just the cheapest one. Fine for both specs'
  assertions and arguably more player-like; the arc now kits out fully, and the
  `MEASURE_DIFF=standard` sink run this session reflected that (coins 1651,
  wagon 97, 0 strand events, capstone reached).

Known notes (unchanged from v11):
- Off-standard difficulty presets (relaxed / demanding) are still illustrative,
  not playtest-balanced.
- `gh pr edit` fails on this repo with a Projects-classic GraphQL error; use
  `gh api -X PATCH .../pulls/N -F body=@file`. Note `gh issue edit` works fine
  (used this session to update #153).

## Next actions (open issues)

1. **#109 fresh full-arc playtest** (owner-gated). The UI + onboarding pass is
   now fully in; a fresh playtest is the natural next signal, and it gates #153.
2. **#151 bigger-maps geography** to fix the difficulty-curve falloff (later maps
   wear the wagon less). Separate, larger map-design pass; feeds #110.
3. **#110 economy tightening** (standard still reads loose on coins).
4. **#152 art Phase 2** - deferred, revisit after the fixes.
5. Still owner-gated from before: **#111** reveal-hidden routes, **#121** demote
   the Arc.

## Risks or decisions needed

- **Playtest gate.** With #149 complete, several remaining issues (#110, #151,
  #153) are effectively waiting on the #109 playtest signal. Recommend the owner
  run a fresh arc next so the economy/geography tuning has real data to work from.
