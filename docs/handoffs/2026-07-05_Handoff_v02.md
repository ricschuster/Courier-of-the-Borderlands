# Session Handoff: 2026-07-05

## Summary

Shipped the three-item feature backlog from the previous handoff, each as its
own CI-gated PR, then deployed. Work was fanned out to three parallel
sub-agents (one per feature, each in an isolated git worktree) and integrated
by the orchestrator in order, resolving conflicts on the way. The new machine
(Ubuntu on WSL2) was bootstrapped and verified first.

Live game: https://ricschuster.github.io/Courier-of-the-Borderlands/

## Completed

- Fresh-machine setup verified: Node 24, npm ci, Playwright chromium plus its
  system libs (installed once with `sudo npx playwright install-deps chromium`,
  which needs a real terminal for the sudo prompt), gh auth. Full sanity suite
  green (lint, typecheck, 279 unit tests, build, 2 e2e).
- Backlog item 1 (PR #7): per-region ford unlock. Split the global `ford`
  terrain into `ford-greybridge` / `ford-saltreach` with distinct unlock ids,
  added `fordUnlockId` to `Region`, gave Saltreach its own signpost, and added
  a save migration mapping legacy `ford-crossing` to `ford-crossing-greybridge`
  (no save-version bump; snapshot shape unchanged).
- Backlog item 2 (PR #8): cargo types. New pure `src/systems/cargo-types.ts`
  with four categories (letters, goods, rumours, secrets) and a small pay
  modifier each, optional `cargoType` on `Contract`, surfaced on the board and
  at delivery. Orchestrator fix on top: the reputation-perk note now compares
  against the cargo-adjusted base so a cargo modifier no longer shows the perk
  label spuriously.
- Backlog item 3 (PR #9): third region Fenmarch plus multi-gateway travel.
  Generalized `Region.gateway`/`connectsTo` into a `gateways` list so a region
  can link to several neighbors. World is now a chain: Greybridge <-> Saltreach
  <-> Fenmarch (Saltreach is the middle node with two gateways).
- Updated design docs (this PR): `01_core_loop.md` (cargo types, per-region
  ford), `03_regions.md` (gateways list, Fenmarch, ford migration).

## Current state

What works:
- Everything above. `main` is green (CI: check + e2e) and deployed.
- 321 unit tests + 2 Playwright smoke tests pass. Working tree clean.

Architecture (unchanged shape):
- Pure logic in `src/systems/` (no Phaser), unit tested. Phaser rendering/input
  in `src/scenes/map-scene.ts` (the big integration file, ~1100 lines).
  Canonical content in `src/data/`. Regions in `src/systems/region-system.ts`.

Known issues / quirks:
- The known global-ford bug from the last handoff is FIXED (item 1).
- `docs/design/02_milestones.md` is stale: it lists M2/M3 items as unchecked and
  deployment as pending, but those all shipped in the prior session. Not
  touched this session to keep the docs PR focused; worth a cleanup pass.
- The contract board still prints the header "GREYWATER BOARD" in every region
  (pre-existing). Minor cosmetic bug; the board itself works per region.
- No e2e test boots Fenmarch specifically (unit tests cover its map/data). The
  existing smoke tests boot Greybridge and Saltreach-from-save. Adding a
  Fenmarch smoke test is a cheap follow-up.
- Phaser 4 Dependabot bump still intentionally ignored (major rewrite).

## How this session was run (multi-agent)

- Three sub-agents (Sonnet) ran in parallel, each with `isolation: worktree`,
  each committing to a named branch (`feat/per-region-ford`, `feat/cargo-types`,
  `feat/third-region`) without pushing.
- Orchestrator (Opus) reviewed each diff, then integrated in order 1 -> 2 -> 3:
  push branch, open PR, wait for CI (check + e2e), squash-merge, delete branch,
  rebase the next branch on updated main, resolve conflicts, re-run checks.
- Only real conflict was the `SALTREACH_REGION` literal (ford's
  signpost/fordUnlockId vs item 3's gateways list); resolved by keeping both.
- Agent worktrees live under `.claude/worktrees/` and are auto-cleaned; that
  path is NOT in `.gitignore` yet (see next actions).

## Next actions (feature backlog)

Build one per branch/PR, CI-gated:

1. Add `cargoType` to the Fenmarch contracts (left unset to decouple item 3 from
   item 2 during parallel work; they currently fall back to `goods`).
2. Per-region contract board header: use the home settlement's name instead of
   the hardcoded "GREYWATER BOARD".
3. Add a Fenmarch e2e smoke test (boot from a save with `regionId: fenmarch`).
4. Give Fenmarch (or a future region) its own ford + signpost to exercise the
   now-per-region ford mechanic in more than one place.
5. Reconcile `docs/design/02_milestones.md` with reality (mark shipped items).

Housekeeping:
- Add `.claude/worktrees/` to `.gitignore` so agent worktrees can never be
  accidentally committed.

Not in the auto-queue (need human input): audio and juice (asset/licensing),
and an art pass beyond grey-box.

## Risks or decisions needed

- Decide whether to keep authoring regions as a linear chain or move to a hub
  layout now that gateways are a list (the model supports either).
- Same open item as last time: whether to flip `enforce_admins` on branch
  protection (hard gate vs owner break-glass).
