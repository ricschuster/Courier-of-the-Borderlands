# Session Handoff: 2026-07-16 (v28)

## Summary

Continues [v27](2026-07-16_Handoff_v27.md) in the same session. Executed the
whole of #301 (the map-scene decomposition and rule extraction the code review
proposed): three mechanical seams, three PRs, each verified with the full
chromium suite before landing. #301 is closed.

## Completed

- **#310 seam 1: e2e test bridge extracted.** `E2EState`/`CourierE2EApi`, the
  `?e2e`/`?turbo`/`?nowear` switches, and the whole `window.__courier`
  implementation moved to `src/scenes/map-scene-e2e.ts`, reading through a
  narrow `E2EHost` the scene supplies (the DialogueHost pattern). No test
  changes needed: specs reference `globalThis.__courier` untyped and the global
  declaration moved with the module.
- **#311 seam 2: delivery reward composition extracted.**
  `computeDeliveryReward()` in `src/systems/delivery-reward.ts` now owns the
  order: cargo modifier, reconnection premium (with its intermediate round),
  standing bonus, Negotiator cut off the boosted payout, flat bonus coins.
  Tests pin the order with discriminating values (45 -> 54 -> 59 -> +6 -> +30 =
  95) so any reshuffle produces a different total. The scene credits
  `reward.total` and builds its event-log notes from the same breakdown.
- **#312 seam 3: low-condition warning rule extracted.**
  `lowConditionWarning(condition, max, alreadyWarned)` in `wagon-condition.ts`
  returns `'warn' | 'hold' | 'rearm'`; the scene applies the transition and
  owns only the toast. Tests walk a full spell (warn once, hold through
  stranding, re-arm on repair, warn again) and were watched failing with the
  stranding guard removed (trap 1 discipline).

## Current state

- `main` green with all three seams merged. `map-scene.ts` 2,419 -> 2,136
  lines; two new pure system modules with unit tests.
- Unit suite grew 893 -> 905 tests (57 files). Full chromium e2e run locally
  before each of the three PRs, 27 passed each time; CI e2e green on all three.
- release-please PR carries the day's work toward 0.4.1.
- Open issues: 3 pre-existing owner-gated (#226 audio, #228 colorblind palette,
  #229 region editor) + 3 design-calls from v27's review (#298 Salt Runners,
  #299 reconnection notes, #300 overlay movement freeze). No open tech-debt.

## Next actions

1. Owner: the three design-calls (#298, #299, #300). Each is a small PR once
   decided; #300's answer also settles the toast-slot nit noted in it.
2. The optional later seams from #301 (TerrainLayer, HUD view-model assembly,
   contract controller) were deliberately NOT carried into a new issue: the
   forcing size problem is solved and each would need its own justification.
   Open one only if map-scene starts growing again.
3. #228 (colorblind palette) remains the smallest pre-existing owner-gated item.

## Risks or decisions needed

- None new. Carried: `release-please-action@v4` on deprecated Node 20;
  waitForTimeout hard waits in three specs (untracked, low value until one
  flakes).
