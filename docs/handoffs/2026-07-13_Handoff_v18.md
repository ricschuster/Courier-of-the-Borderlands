# Session Handoff: 2026-07-13 (v18)

## Summary

Finished Art Phase 2, then made the repository the durable store for project
context (the owner works across two Claude subscriptions, so Claude-local memory
is not shared or permanent). Repopulated the backlog from memory-only threads
into GitHub issues, cleared the whole build lane (variety + bundle split), and
resolved a stale-memory design conflict before it became wasted work.

## Completed this session

- **#203 shipped** (PR #207): wagon-condition meter on the terrain row (Kenney UI
  bar, deterministic green/amber/red fill + tinted n/n), repair prompt moved to
  the bottom hint line, terrain line slimmed. **Art Phase 2 is now complete.**
- **Durable-context migration** (PR #214): new CLAUDE.md section "Where durable
  context lives" (repo = single source of truth: issues, CLAUDE.md, docs/), plus
  the autonomous one-PR-per-feature + `--squash --auto` preference. Parked
  memory-only threads opened as issues **#208-#213**, so the backlog is real
  again rather than living in personal memory.
- **#208 closed, not built.** It proposed reveal-based hidden *routes*, but
  `docs/design/07` records that idea was already evaluated and rejected in the
  #111 pass (owner call): an invisible shortcut fights the "desire a visible
  route" pull, and reveal is hard to keep the arc honest about. Shipped instead
  as reveal-rewarded *lore* (#111). The repo-as-source-of-truth caught this on
  its first use, right after the migration.
- **#209 shipped** (PR #215): deterministic per-tile terrain variety.
  `terrainTileArt(id, x, y)` picks a same-family frame + horizontal flip from a
  salted coordinate hash; flip only on non-directional ground. Grass/forest and
  the wetland grounds now read varied instead of a stamp grid.
- **#210 shipped** (PR #216): Phaser split into its own bundle chunk. App chunk
  dropped from 1,320 KB (gzip 359) to 122 KB (gzip 40.6); the engine chunk stays
  cached across app-only deploys. Warning limit right-sized to 1400.

## Current state

- **What works:** `main` is green (791 unit tests + full chromium e2e across all
  three regions). Each slice was verified in-browser. The terrain skin, wagon
  bar, contract-board frame, tile variety, and split bundle all render/boot
  clean.
- **Backlog:** the build lane is empty. Remaining open issues are all
  **playtest-gated**: #211 (fresh full-arc playtest), #212 (world-reacts mission
  step), #213 (economy tightening).
- **Process change worth carrying:** stop relying on personal memory for durable
  facts; write to issues / CLAUDE.md / docs (see the new CLAUDE.md section). This
  applies on both subscriptions because it is in the repo.

## Next actions

1. **A human full-arc playtest (#211)** is the highest-leverage next move: build
   work has hit diminishing returns, several deferred threads want a human read,
   and there is unvalidated new work stacked since the last playtest (#207
   wagon bar, #209 variety, and the earlier art). Paste notes -> triage skill.
2. If not ready to test, the only sizeable build option left is a new region /
   bigger content chunk, but hold it until a playtest confirms existing content
   lands.
3. Then act on whatever #211 surfaces (likely feeds #212 / #213).

## Risks or decisions needed

- No open risks. The one decision waiting is the owner's: playtest now, or start
  a new-region content chunk. Recommendation is playtest first.
